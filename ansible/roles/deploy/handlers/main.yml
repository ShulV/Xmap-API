- name: Restart only app (handler)
  become: yes
  ansible.builtin.shell: |
    cd {{ deploy_path }}
    podman-compose stop app
    podman-compose rm app
    podman-compose up app -d --build
  args:
    chdir: "{{ deploy_path }}"